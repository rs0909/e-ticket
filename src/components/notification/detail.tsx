{/*
import React from 'react';

import { NotificationType } from '@/pages/notification';

export interface PropType {
  notifications: NotificationType[];
}


function Detail({ notifications }: { notifications: NotificationType[] }) {
  return (
    <div className="container my-3">
      <h2 className="border-bottom py-2">{notifications.title}</h2>
      <div className="card my-3">
        <div className="card-body">
          <div className="card-text" style={{ whiteSpace: 'pre-line' }}>
            {notifications.message}
          </div>
          <div className="d-flex justify-content-end">
            <div className="badge bg-light text-dark p-2">{notifications.updated_at}</div>
          </div>
        </div>
      </div>
    </div>
  );
};

export default Detail;
*/}